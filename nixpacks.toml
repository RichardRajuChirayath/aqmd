# Railway Nixpacks Configuration for AQMD

[phases.setup]
nixPkgs = ["nodejs_20", "openssl"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
    "npx prisma generate",
    "npm run build"
]

[deploy]
cmd = "npx prisma migrate deploy && npm run start"
